import React from "react";

const FormButton = () => {
    return (
        <button type="submit">
            <i className="fa-solid fa-magnifying-glass"></i>
        </button>
    );
};

export default FormButton;
